version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "80:80"
    networks:
      - payment-net

  backend:
    build:
      context: ./backend
    ports:
      - "8080:8080"
    networks:
      - payment-net

networks:
  payment-net:
    driver: bridge
